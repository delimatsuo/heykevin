.env
.env.*
secrets/
*.p8
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/
.playwright-mcp/
gha-creds-*.json
.worktrees/
# Claude Code's built-in worktree tool uses this path instead of .worktrees/.
# Scoped to the subdirectory on purpose: .claude/deploy-config.yaml is tracked.
.claude/worktrees/
# Local-only: CI installs with `pip install -e ".[dev]"` and never reads this
# lockfile, so committing it would imply a reproducibility guarantee CI doesn't
# enforce. Pinning CI installs is a deliberate workflow change, not this file.
uv.lock
.DS_Store
HANDOFF*.md
HANDOFF-*.md

# Xcode user-local state.
ios/Kevin.xcodeproj/xcuserdata/
ios/Kevin.xcodeproj/project.xcworkspace/xcuserdata/
ios/DerivedData*/

# Local App Store marketing automation artifacts.
scripts/appstore-screenshots/
scripts/cpp_ids.json
scripts/create_custom_product_pages.py
scripts/update_promo_text.py
# Generated screenshot output only. The capture script itself IS tracked: it and
# ios/Kevin/Debug/AppStoreScreenshotFixtures.swift are what regenerate these
# PNGs, and the script previously existed in exactly one throwaway worktree with
# no copy in git — ignoring it there would have destroyed the only copy.
marketing/app-store/screenshots/

# Session screenshots referenced by docs/handoffs — kept local, not for the repo
jobber-login.png
live-screen-1.png
live-screen-2.png
recents.png
