# Offline native-audio qualification resumption V1

## Binding

- Worktree: `/Volumes/Extreme Pro/myprojects/Kevin/.worktrees/voice-architecture-bakeoff-plan`
- Branch: `codex/voice-architecture-bakeoff-plan`
- Source SHA: `2ed8ea7d1d7f338e84ddf08d5a50a714835e1533`
- Product goal: prove the native-audio call-screening architecture with deterministic
  offline evidence before any isolated nonproduction provider capability probe.
- Authority: fixture-only inspection, tests, lint, and dry-run rejection evidence.

This brief is not a materialization, provider, credential, PSTN, staging,
production, deployment, or Task 4.8 authorization.

## Frozen Phase 0.5 boundary

The Phase 0.5 administrative lane is frozen as blocked, non-authorizing evidence
at this exact V10 five-file bundle:

| File | SHA-256 |
| --- | --- |
| `docs/security/voice-bakeoff-environment-reconciliation-phase-0-5-v10.json` | `e2dc17aa37e8910cf656df00b753083b930ad192a41d83b8254c016a8d159365` |
| `docs/security/voice-bakeoff-environment-reconciliation-phase-0-5-v10.schema.json` | `3c9a0a2d6fef7d33257a7bf3b8864bd7f218f265b14bd472d5cca15898f819af` |
| `docs/security/voice-bakeoff-environment-reconciliation-phase-0-5-v10.md` | `a38f989576ca363434f641fb352b3dbc73dbd39847f4a42633eef028c9aead98` |
| `scripts/verify_voice_bakeoff_phase_one_admission_v10.py` | `d6ed4287af19d86d269ce694c7a63903903b7f2cfe712dd665c4f718d0cffb2b` |
| `tests/unit/test_voice_bakeoff_environment_reconciliation_phase_0_5_v10.py` | `e288199d8d40b357a949a8cee0653197fb9512d41bc5e919c683136152916d6b` |

Staff accepted the exact bundle. Security/privacy and operator accepted it with
the conditions below. V10 verifier exit `0` means only
`verified_blocked_contract`; it is never readiness, admission, native-audio,
caller, provider, deployment, or product evidence. A future successor must avoid
the ambiguous phrase `payload_usable` and distinguish a complete restricted
retention envelope from execution usability.

## Unchanged seals

The resumed lane must preserve all of these as hard stops:

- no V9 materializer invocation and no real materialization/private-input records;
- no credentials, identity resolution, secrets, provider account, or provider API;
- no Twilio/PSTN, callback, WebSocket, telephony number, or manual call;
- no staging, production, deployment, Cloud Run, Firestore, RTDB, or external write;
- no Task 4.8 capability probe and no Stage 5 execution;
- no provider, language, latency, caller-heard, live-control, or product-success claim;
- no change to protected `docs/handoffs/` artifacts.

The deny-only gate report must remain `not_authorized`, with credential resolution,
networking, provider, and PSTN access blocked.

## Bounded offline task

Requalify the two native-audio arms and their shared isolated harness:

- Arm A remains a non-selectable native-quality control. Offline evidence may
  prove typed lifecycle mapping, tools/terminal denial, bounded usage, raw-payload
  non-retention, and mocked permit ordering only.
- Arm C remains a feasibility probe. Offline evidence may prove explicit
  activity ordering, planner permit before mocked audio, stale-epoch rejection,
  bounded generation timeout, and total typed lifecycle mapping only.
- The isolated application and caller harness may prove route/import isolation,
  deterministic schedules, common-clock bookkeeping, cap enforcement,
  cancellation, encrypted temporary evidence teardown, and refusal without an
  externally verified approval.

No valid-looking real approval, sealed provider manifest, private value, or
credential may be created to exercise this task. Synthetic objects that live only
inside unit-test temporary directories remain fixture evidence, not authority.

## Success evidence

The bounded task succeeds only if:

1. Arm A, Arm C, shared speech/lifecycle/coordinator, app-isolation, caller-harness,
   and dry-run preflight tests all pass without network access.
2. Ruff passes for the exact source and test set.
3. The repository template/absent-approval CLI path rejects locally and never
   contacts the offline harness or any external dependency.
4. The fresh deny-only gate report remains `not_authorized` with all nine blockers.
5. Results are labeled offline fixture evidence only.

Passing this task closes no external prerequisite and cannot advance to Task 4.8.
