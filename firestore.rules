rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all client-side access. All access via Admin SDK only.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
